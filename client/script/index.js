function redirectTo(page) {
  if (page === "register") {
    window.location.href = "register.html"; // Replace with actual route
  } else if (page === "login") {
    window.location.href = "login.html"; // Replace with actual route
  }
}
